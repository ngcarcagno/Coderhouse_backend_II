/**
 * DTOs INDEX
 * Exporta todos los DTOs para facilitar la importación
 */

const UserDTO = require("./user.dto");
const ProductDTO = require("./product.dto");
const CartDTO = require("./cart.dto");
const TicketDTO = require("./ticket.dto");

module.exports = {
  UserDTO,
  ProductDTO,
  CartDTO,
  TicketDTO,
};
